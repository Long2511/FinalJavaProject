package Calculator;

public class VerifyTheResult {
    public boolean VerifyTheResult(int calculatedAnswer, int userAnswer){
        if (calculatedAnswer == userAnswer) return true;
        else return false;
    }
}
