package Calculator;

public class VerifyTheResult {
    public static boolean VerifyTheResult(int calculatedAnswer, int userAnswer){
        if (calculatedAnswer == userAnswer) return true;
        else return false;
    }
}
